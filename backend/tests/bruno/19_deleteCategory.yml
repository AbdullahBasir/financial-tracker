info:
  name: 19_deleteCategory
  type: http
  seq: 19

http:
  method: DELETE
  url: "{{baseUrl}}/categories/{{categoryId}}"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        expect(res.status).to.equal(204);

        console.log('Category deleted');

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
