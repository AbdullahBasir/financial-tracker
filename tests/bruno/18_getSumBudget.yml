info:
  name: 18_getSumBudget
  type: http
  seq: 18

http:
  method: GET
  url: "{{baseUrl}}/budgets/summary?month=2026-08"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
    - name: Content-Type
      value: application/json
  params:
    - name: month
      value: 2026-08
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
